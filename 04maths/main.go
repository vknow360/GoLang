package main

import (
	"fmt"
	"math/rand"
)

//"math/rand"

func main() {

	// random from math
	//rand.Seed(time.Now().UnixNano() / 2)
	fmt.Println(rand.Intn(5) + 1)

	// random from crypto

	// \

}
